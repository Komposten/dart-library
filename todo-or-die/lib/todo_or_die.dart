/// Write TODOs in code that ensure you actually do them.
///
/// Use [todoOrDie] to mark a TODO with a due date, a condition, or both.
/// When the TODO is due, a [TodoIsDueError] is thrown (or a custom
/// [TodoOrDie.configure] callback is invoked).
library;

export 'src/todo_or_die_base.dart';
