name: todo_or_die
description: Write TODOs in code that ensure you actually do them, inspired by searls/todo_or_die
version: 1.0.0
repository: https://github.com/komposten/dart-library

environment:
  sdk: ^3.12.2

# dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
