name: aside
description: Convenient and simple cross-platform isolate runners
version: 1.0.0
repository: https://github.com/komposten/dart-library

environment:
  sdk: ^3.12.2

# dependencies:

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.6
